pipeline {
    agent any

    tools { 
        jdk 'JAVA_HOME' 
        maven 'M2_HOME' 
    }

    environment {
        DB_URL = 'jdbc:mysql://localhost:3306/stationSki'
        NEXUS_REPO_URL = "http://127.0.0.1:8081/repository/maven-releases/"
        MAVEN_SETTINGS = "/usr/share/maven/conf/settings.xml"
    }
    
    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'hadil', url: 'https://github.com/lolly1259998/devops.git'
            }
        }

        stage('Compile Stage') {
            steps {
                sh 'mvn clean install -DskipTests'
            }
        }

        /*stage('SonarQube Analysis') {
            steps {
                sh '''
                    mvn sonar:sonar \
                        -Dsonar.token=squ_186107db379329e447e8d1a34c132010eeb8c8c4 \
                        -Dmaven.test.skip=true
                '''
            }
        }*/

       /*stage('Deploy to Nexus') {
            steps {
                sh 'mvn deploy -DskipTests --settings /usr/share/maven/conf/settings.xml'
            }
        }
    }*/

    post {
        success {
            echo "Pipeline completed successfully!"
        }
        failure {
            echo "Pipeline failed. Please check the logs for details."
        }
    }
}
